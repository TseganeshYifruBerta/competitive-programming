class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        digits = [str(digits[i]) for i in range(len(digits))]
        integer = int(''.join(digits))
        return str(integer + 1)
